prototype(NeosRulez.Neos.Form.FormHandlerFinisher:FormHandlerFinisher.Definition) < prototype(Neos.Form.Builder:Finisher.Definition) {
    formElementType = 'NeosRulez.Neos.Form.FormHandlerFinisher:FormHandlerFinisher'
    options {
        endpoint = ${null}
        node = ${node}
    }
}
